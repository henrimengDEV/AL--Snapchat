import 'package:flutter/cupertino.dart';

class ScreenUsername extends StatelessWidget {
  const ScreenUsername({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
